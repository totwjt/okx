FROM freqtradeorg/freqtrade:stable

# Ensure requests-based paths (used by some ccxt sync endpoints) can use socks5h proxy.
# The strategy registry CLI also needs PostgreSQL access when run inside the image.
RUN pip install --no-cache-dir "requests[socks]" pysocks psycopg2-binary PyYAML
